SELECT artists, COUNT(artist)
FROM torrents
GROUP BY artist
ORDER BY COUNT(artist) DESC
LIMIT 5;

--Updated Query
SELECT artist, COUNT(artist)
FROM torrents
GROUP BY artist
ORDER BY COUNT(artist) DESC
LIMIT 6;

